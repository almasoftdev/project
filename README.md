## This is a django project for admin tasks

this is built as a monolith, it will be done using django templates engine, with django-cotton to simplify the component syntax, as well as basecoatUI for shadcn-type stylings
